Automodel method gets the config class for the model.
It uses the config class to get the model config and model file. 
Combining the model class and model random config, it initializes the model class with a random config.
Finally model object is added with model file to set the weights to the pretrained model values.